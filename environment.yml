name: base
channels:
  - defaults
  - conda-forge
dependencies:
  - matplotlib
  - python=3.11.10
  - jupyterlab
  - xarray
  - cartopy
  - notebook
  - pandas==2.3.3
  - scipy
  - pip:
    - xskillscore 
    - bottleneck
    - torch==2.5.1
prefix: /opt/conda
  
  
  

